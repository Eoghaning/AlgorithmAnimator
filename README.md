# AlgorithmAnimator
This project is my Algorithm Animator it visually shows how different algorithms work step by step. It uses an array displayed on screen, where each value is represented visually so the user can see how elements move, compare, and change during sorting or searching, depending on algorithm type.
